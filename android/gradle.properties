# Enable AndroidX support in Gradle (required for Jetpack libraries like webkit)
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX if needed
android.enableJetifier=true
